---
- name: Execute curl command
  hosts: all
  connection: local
  gather_facts: false
  tasks:
    - name: Get machine IP address
      shell: hostname -I
      register: ip_address

    - name: Execute curl command
      command: curl {{ ip_address.stdout }}
